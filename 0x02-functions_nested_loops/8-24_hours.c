#include "main.h"

/**
 * lack_bauer - prints every minute of the day from 00:00 to 23:59
 *
 * Return: ALways return 0.
 */

void jack_bauer(void)
{
	int a;
	int b;

	if (a = 0; a <= 23; a++)
	{
	if (b = 0; b <= 59; b++)
	}
	{
	_putchar (a / 10 + '0');
	_putchar (a % 10 + '0');
	_putchar (':');
	_putchar (b / 10 + '0');
	_putchar (b % 10 + '0');
	_putchar ('\n');
	}
}
