#include "main.h"
#include <stdio.h>

/**
 * addition -adds two int and prints result
 *
 * @l: first int value
 * @m: second int value
 * Return: Always return 0.
 */

int add(int l, int m)
{
	return (l + m);
}
