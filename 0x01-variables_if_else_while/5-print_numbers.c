#include <stdio.h>

/**
 * main - Entry 
 *
 * return: Always return 0 (Success)
 */

int main(void)
{
	int num_base10;

	for (num_base10 = 0; num_base10 < 10; num_base10++)
		printf("%d\n", num_base10);
	putchar('\n');
	return (o);
}
